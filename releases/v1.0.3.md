> **非公式・非提携**：本プロジェクトは個人による非公式の開発です。Rokid社およびその関連会社とは提携しておらず、承認・支援・スポンサー提供を受けていません。
> **Unofficial and unaffiliated**: This is an independent, unofficial project. It is not affiliated with, endorsed, supported, or sponsored by Rokid or its affiliates.

## 日本語 — 無期限WAITINGの解消と入力診断

Rokid Glasses向けオーディオスペクトルアナライザの**プレリリース**です。

- 無期限のWAITINGを有限回の入力検査へ変更。MIC／VOICEの16 kHz候補など、対応時は最大9プロファイルを試す。
- 開始時のポリシー無音化は約0.8秒、LIVE後の割り込みは約4秒の猶予後に次の候補へ進む。
- Android 11以降で非排他設定を確認してから開始し、要求が反映されない入力は使わない。入力デバイスの固定選択を撤廃。
- 全候補の検査後はマイクを解放し、MIC BLOCKED／NO SIGNAL／MIC ERRORと検査件数を表示。
- WAITINGや入力エラー画面のタップをRETRYに変更。アシスタント表示時のマイク解放・復帰とHOLD維持は継続。

**既知の制限:** 1.0.3の実機でのマイク取得とアシスタント併用は未検証です。全ての非排他入力を端末が拒否する場合、APKだけで同時取得を強制することはできません。初回の確認はグラスの録画・音声付き画面共有を止め、INPUT: AUTOで行ってください。

検証: 数値・状態29件、マイク制御32件、Activity 14件、描画17状態。Android APIの代替実装／Java2Dでの検証であり、実機での実音解析の成功を保証しません。

Assetsの`RokidSpectrum-1.0.3.apk`をインストールし、マイクを許可してください。API 26以上。以前の版と同じ署名・パッケージ名です。DEMOは合成信号、dBFSはdB SPL／dBAではありません。

## English — Bounded input probing and WAITING diagnostics

An experimental **prerelease** of the microphone audio spectrum HUD for Rokid Glasses.

- Replaces indefinite WAITING with a finite scan of up to nine supported profiles, including explicit 16 kHz MIC/VOICE probes.
- Advances after about 0.8 seconds of initial policy silencing, or a roughly 4-second grace period after LIVE capture.
- Verifies non-private capture before recording on Android 11+, rejects ignored requests and leaves device routing to Android.
- Releases all recorders after exhaustion and reports MIC BLOCKED, NO SIGNAL or MIC ERROR with diagnostic counts.
- Makes taps on WAITING/input errors retry capture while preserving assistant focus handling and user HOLD.

**Known limitations:** Physical-device microphone capture and assistant coexistence remain unverified in 1.0.3. The APK cannot force shared capture if the device blocks every non-private input. For the first device check, stop glasses recording/audio screen sharing and use INPUT: AUTO.

Verification: 29 numerical/state tests, 32 audio lifecycle tests, 14 Activity tests and 17 render states. These use Android framework doubles and Java2D; they do not establish successful live capture on hardware.

Install `RokidSpectrum-1.0.3.apk` from Assets and allow microphone access. Requires API 26+. The APK retains the same original signing certificate and package name as earlier versions. DEMO is synthetic; dBFS is not dB SPL/dBA.

## インストール / Installation

```sh
adb install -r -g RokidSpectrum-1.0.3.apk
adb shell am start -n dev.xenoah.rokidspectrum/dev.xenoah.spectrum.MainActivity
```

[この版のREADME / Version README](https://github.com/Xenoah/rokid-spectrum-hud/blob/v1.0.3/README.md)

APKは元の署名済みファイルです。ソースZIPはこの公開コミットから生成し、元のアプリコードに日英ドキュメントと写真を加えています。SHA256SUMS.txtで添付ファイルを確認できます。秘密鍵は含みません。
The APK is the original signed build. The source ZIP is generated from this published commit, with unchanged application code plus bilingual documentation and photos. SHA256SUMS.txt covers the attached files. No private signing key is included.

## 画面 / Screenshots


**合成入力・共有描画コードのプレビュー（実機画像ではありません） / Synthetic shared-renderer preview, not a device screenshot**

![合成入力・共有描画コードのプレビュー（実機画像ではありません） / Synthetic shared-renderer preview, not a device screenshot](https://github.com/Xenoah/rokid-spectrum-hud/releases/download/v1.0.3/RokidSpectrum-1.0.3-demo-preview.png)

**合成入力・共有描画コードのプレビュー（実機画像ではありません） / Synthetic shared-renderer preview, not a device screenshot**

![合成入力・共有描画コードのプレビュー（実機画像ではありません） / Synthetic shared-renderer preview, not a device screenshot](https://github.com/Xenoah/rokid-spectrum-hud/releases/download/v1.0.3/RokidSpectrum-1.0.3-input-state-preview.png)

**合成入力・共有描画コードのプレビュー（実機画像ではありません） / Synthetic shared-renderer preview, not a device screenshot**

![合成入力・共有描画コードのプレビュー（実機画像ではありません） / Synthetic shared-renderer preview, not a device screenshot](https://github.com/Xenoah/rokid-spectrum-hud/releases/download/v1.0.3/RokidSpectrum-1.0.3-scanning-preview.png)
